# O npm 10.9 quebra ("Cannot read properties of null (reading 'edgesOut')")
# ao resolver as peer dependencies opcionais do vitest 5. As peers deste
# projeto (vite do Astro, typescript 6) são declaradas à mão.
legacy-peer-deps=true
